export type NutritionApproach = "orange_base" | "meal_bank";

/** Per-day evening meal approach; null means not chosen yet. */
export type EveningApproach = "meal_bank" | "orange_base" | "own" | null;
