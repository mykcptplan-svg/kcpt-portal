import type { WeeklyTrackerEntry } from "@/types";

export async function getWeeklyTracker(/* TODO: args */): Promise<WeeklyTrackerEntry | void> {
  // TODO: fetch Edge Function with Authorization: Bearer <token>
}
